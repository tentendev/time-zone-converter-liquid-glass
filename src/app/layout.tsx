import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Time Zone Converter - Beautiful Time Zones at a Glance",
  description: "A modern, elegant time zone converter with Apple's Liquid Glass UI design. Track multiple time zones, handle DST automatically, and enjoy smooth animations.",
  keywords: "time zone, converter, world clock, timezone, UTC, DST, time difference",
};

export const viewport = {
  width: 'device-width',
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
